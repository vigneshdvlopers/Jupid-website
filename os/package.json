{
  "name": "os-project",
  "version": "1.0.0",
  "description": "A single-page React application",
  "main": "index.html",
  "scripts": {
    "start": "serve .",
    "build": "mkdir -p dist && cp index.html dist/"
  },
  "dependencies": {
    "serve": "^14.2.0"
  }
}
